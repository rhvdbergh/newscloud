.jumbotron.jumbotron-fluid
    .container
      h1.display-4.text-center.text-white NewsCloud
      p.lead.text-center.text-white
        if searchTerm
          | Word clouds generated by searching "#{searchTerm}" on regular news outlets and Twitter.
        else
          | Compare regular news outlets with Twitter on any given search term.